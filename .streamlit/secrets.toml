#  Streamlit secrets template
# --- 1. USER LOGIN (The "Gatekeeper") ---
[auth]
# SHARED: Use the same Client ID for all apps
client_id = "105092..." 
client_secret = "YOUR_CLIENT_SECRET"
# CHANGE THIS URL depending on which app you are running!
redirect_uri = "http://localhost:8501" 

# --- 2. BACKEND ACCESS (The "Storage Admin") ---
[google_auth]
# SHARED: Use the same Refresh Token for all apps
client_id = "105092..." 
client_secret = "YOUR_CLIENT_SECRET"
refresh_token = "YOUR_SHARED_Y4JAIOPS_REFRESH_TOKEN"

# --- 3. AI BRAIN ---
[gemini]
api_key = "YOUR_GEMINI_API_KEY"

# --- 4. APP SPECIFIC SETTINGS ---
[youthscan]
folder_id = "1Vavl3N..." # Folder for Candidate Resumes

[youthjobs]
folder_id = "INSERT_FOLDER_ID_FOR_JOBS_HERE" # Folder for Job Descriptions
